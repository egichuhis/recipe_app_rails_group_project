<!DOCTYPE html>
<html>
  <head>
    <title>RecipeAppRailsGroupProject</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <% if user_signed_in? %>
    <div class="container">
    <!-- Navbar with User label, Logout button, and Buttons -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light d-flex justify-content-between">

      <!-- Buttons -->
      <div class="d-flex gap-4">
        <button type="button" class="btn btn-primary mx-2">Recipes</button>
        <button type="button" class="btn btn-primary mx-2">Foods</button>
        <button type="button" class="btn btn-primary mx-2">My Recipes</button>
      </div>

      <div class="d-flex gap-4">
        <!-- User label -->
        <p class="navbar-text text-muted mr-3">
          Welcome, <%= @current_user.name %>
        </p>

        <!-- Logout button -->
        <form class="form-inline my-2 mx-2">
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-sm btn-danger" %>
        </form>

      </div>
      
    </nav>
    </div>
  <% end %>

  <div>
    <!-- Notice and Alert messages -->
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </div>

    <%= yield %>
  </body>
</html>
